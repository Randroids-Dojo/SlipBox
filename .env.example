# Shared secret for authenticating inbound API requests (required)
SLIPBOX_API_KEY=

# Password for the browser-based graph UI (required)
SLIPBOX_UI_PASSWORD=

# Secret for signing browser session tokens — generate with: openssl rand -hex 32
SESSION_SECRET=

# OpenAI API key for generating embeddings
OPENAI_API_KEY=

# GitHub fine-grained personal access token (needs Contents read/write on PrivateBox repo)
GITHUB_TOKEN=

# PrivateBox repository owner (GitHub username or org)
PRIVATEBOX_OWNER=

# PrivateBox repository name
PRIVATEBOX_REPO=

# --- Optional overrides (defaults shown) ---

# EMBEDDING_MODEL=text-embedding-3-large
# SIMILARITY_THRESHOLD=0.82
# GITHUB_API_BASE=https://api.github.com
# NOTES_DIR=notes
# EMBEDDINGS_INDEX_PATH=index/embeddings.json
# BACKLINKS_INDEX_PATH=index/backlinks.json
# CLUSTERS_INDEX_PATH=index/clusters.json
# MIN_CLUSTERS=2
# MAX_CLUSTERS=20
# KMEANS_MAX_ITERATIONS=50
# MIN_NOTES_FOR_CLUSTERING=3
# TENSIONS_INDEX_PATH=index/tensions.json
# TENSION_THRESHOLD=0.72
# MIN_NOTES_FOR_TENSION=4
